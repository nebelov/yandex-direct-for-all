# Установка

    bash scripts/install_bundle.sh --target codex
    bash scripts/install_bundle.sh --target codex --apply

Допустимы цели codex, claude и both. Без --apply команда ничего не записывает. После применения она печатает RUN_ID и команду точного отката.
