[project]
name = "yandex-search-mcp"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "mcp[cli]>=1.0.0",
    "requests",
    "python-dotenv"
]

[project.scripts]
yandex-search = "server:main"
